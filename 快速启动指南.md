# 快速启动Redis指南

## 🚀 最简单的方式：使用Docker（推荐）

### 步骤1：启动Docker Desktop
1. 在Windows开始菜单找到并打开 **Docker Desktop**
2. 等待Docker完全启动（系统托盘图标变为绿色，通常需要30秒-1分钟）

### 步骤2：启动Redis容器

**方式A：使用批处理脚本（最简单）**
```bash
# 双击运行 start-redis.bat
# 或在命令行执行：
start-redis.bat
```

**方式B：直接使用Docker命令**
```bash
docker run -d --name student-hub-redis -p 6379:6379 redis:7-alpine
```

**方式C：使用docker-compose（推荐，可以同时启动MySQL和Redis）**
```bash
docker-compose up -d mysql redis
```

### 步骤3：验证Redis是否运行
```bash
# 检查容器状态
docker ps | findstr redis

# 测试Redis连接
docker exec -it student-hub-redis redis-cli ping
# 应该返回: PONG
```

### ✅ 完成！
现在可以启动Spring Boot应用了：
```bash
cd backend
mvn spring-boot:run
```

---

## 📦 备选方案：安装Windows版Redis（不推荐）

如果你真的不想使用Docker，可以安装Windows版本的Redis，但**不推荐**因为：
- 官方不再维护Windows版本
- 配置复杂
- 性能不如Docker版本

如果仍要安装：

### 选项1：使用Memurai（Redis的Windows替代品）
1. 访问：https://www.memurai.com/
2. 下载并安装Memurai
3. 安装后会自动作为Windows服务运行
4. 默认端口就是6379，无需修改配置

### 选项2：使用WSL2中的Redis
如果你安装了WSL2：
```bash
# 在WSL2终端中执行
sudo apt update
sudo apt install redis-server -y
sudo service redis-server start
```

### 选项3：使用旧版Redis for Windows
- 下载地址：https://github.com/microsoftarchive/redis/releases
- ⚠️ 注意：此项目已停止维护

---

## 🔧 常用Docker命令

```bash
# 启动Redis
docker start student-hub-redis

# 停止Redis
docker stop student-hub-redis

# 查看Redis日志
docker logs student-hub-redis

# 重启Redis
docker restart student-hub-redis

# 删除Redis容器
docker stop student-hub-redis
docker rm student-hub-redis
```

---

## ❓ 常见问题

**Q: Docker Desktop启动很慢怎么办？**
A: 首次启动需要时间，之后会快很多。确保WSL2已正确安装。

**Q: 端口6379已被占用？**
A: 检查是否有其他Redis实例在运行，或修改docker-compose.yml中的端口映射。

**Q: 使用Docker会影响性能吗？**
A: 不会，Docker在Windows上性能很好，特别是使用WSL2后端时。

